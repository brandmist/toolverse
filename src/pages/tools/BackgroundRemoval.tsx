// This standalone page is deprecated — Background Removal is handled 
// directly inside ToolDetail.tsx via the RemoveBackground component.
// Keeping this file as a redirect to avoid broken imports.

export const BackgroundRemoval = () => null;
